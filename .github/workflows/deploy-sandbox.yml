name: Deploy to Sandbox (Montonio Test)

on:
  workflow_dispatch:
    inputs:
      confirmation:
        description: 'Type "SANDBOX" to confirm deployment with test payments'
        required: true
        default: ''

jobs:
  deploy-sandbox:
    runs-on: ubuntu-latest

    env:
      APP_ROOT: /data01/virt133730/domeenid/www.catshelp.ee
      RELEASES_DIR: /data01/virt133730/domeenid/www.catshelp.ee/annetamistalgud/releases
      PM2_APP_NAME: annetamistalgud-sandbox
      NODE_ENV: development

    steps:
      - name: ✅ Check confirmation
        run: |
          if [ "${{ github.event.inputs.confirmation }}" != "SANDBOX" ]; then
            echo "❌ Sandbox deployment cancelled - you must type 'SANDBOX' to confirm"
            exit 1
          fi
          echo "✅ Sandbox deployment confirmed"
          echo "🧪 Deploying with TEST Montonio integration"

      - name: 🧾 Checkout repository
        uses: actions/checkout@v3

      - name: 📦 Install dependencies
        run: npm install

      - name: 🏗️ Build frontend & backend
        run: npm run build

      - name: 🛠️ Prisma generate & copy
        run: npm run copy:prisma

      - name: 📁 Prepare release archive
        run: |
          TIMESTAMP=$(date +%Y.%m.%d-%H.%M.%S)
          GIT_SHA=$(git rev-parse --short HEAD)
          RELEASE_NAME=sandbox-$TIMESTAMP-$GIT_SHA
          echo "RELEASE_NAME=$RELEASE_NAME" >> $GITHUB_ENV
          tar -czf $RELEASE_NAME.tar.gz dist
          echo "📦 Release archive created: $RELEASE_NAME.tar.gz"

      - name: 📤 Upload release to server
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.CATSHELP_SSH_KEY }}
          port: 22
          source: "${{ env.RELEASE_NAME }}.tar.gz"
          target: "${{ env.RELEASES_DIR }}"

      - name: 🚀 Deploy and restart via PM2
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.CATSHELP_SSH_KEY }}
          port: 22
          script: |
            set -e

            cd ${{ env.RELEASES_DIR }}
            mkdir -p ${{ env.RELEASE_NAME }}
            tar -xzf ${{ env.RELEASE_NAME }}.tar.gz -C ${{ env.RELEASE_NAME }}
            rm ${{ env.RELEASE_NAME }}.tar.gz

            # Symlink new release to current
            ln -sfn ${{ env.RELEASES_DIR }}/${{ env.RELEASE_NAME }}/dist ${{ env.APP_ROOT }}/annetamistalgud

            # Copy Google Sheets credentials if they exist
            if [ -f "${{ env.APP_ROOT }}/credentials.json" ]; then
              cp ${{ env.APP_ROOT }}/credentials.json ${{ env.APP_ROOT }}/annetamistalgud/credentials.json
              echo "✅ Google Sheets credentials copied"
            fi

            # Restart via PM2 (expects ecosystem config in release)
            cd ${{ env.APP_ROOT }}/annetamistalgud/dist
            pm2 startOrRestart ${{ env.APP_ROOT }}/annetamistalgud/pm2.config.js --only ${{ env.PM2_APP_NAME }}

            # TODO: Add cleanup step once pipeline works flawlessly
            # Cleanup old sandbox releases (keep last 3)
            # cd ${{ env.RELEASES_DIR }}
            # ls -t | grep "^sandbox-" | tail -n +4 | xargs -r rm -rf
            # echo "🧹 Cleaned up old sandbox releases"

            echo "✅ Sandbox deployment completed: ${{ env.RELEASE_NAME }}"
            echo "🧪 Test at: https://test.catshelp.ee"
            echo "✅ Montonio SANDBOX payments are active (test mode)"

      - name: 📊 Deployment Summary
        run: |
          echo "## 🧪 Sandbox Deployment Successful" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Release:** \`${{ env.RELEASE_NAME }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Environment:** Sandbox (TEST Montonio payments)" >> $GITHUB_STEP_SUMMARY
          echo "**URL:** https://test.catshelp.ee" >> $GITHUB_STEP_SUMMARY
          echo "**Deployed:** $(date -u +'%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### ℹ️ Testing" >> $GITHUB_STEP_SUMMARY
          echo "- Test payments will use Montonio sandbox" >> $GITHUB_STEP_SUMMARY
          echo "- No real money will be processed" >> $GITHUB_STEP_SUMMARY
          echo "- Use test bank credentials from Montonio" >> $GITHUB_STEP_SUMMARY
          echo "- Check PM2 logs: \`pm2 logs ${{ env.PM2_APP_NAME }}\`" >> $GITHUB_STEP_SUMMARY
